# TODO

## Operations

* Add a GitHub Actions deployment workflow only after a deployable application and package manager file exist.
* Add Wrangler configuration only when Cloudflare Worker bindings are known.
* Document any future Cloudflare KV, R2, D1, or Durable Object resources before creating them.
