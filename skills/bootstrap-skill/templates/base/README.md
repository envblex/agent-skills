# Project Name

Briefly describe the project and the current approved scope.

## Documents

- `SPEC.md` defines the current implementation scope.
- `TODO.md` tracks deferred work.
- `AGENT.md` defines repository rules for future agents.
- `README.md` explains how to use the generated project.

## Setup

Add project-specific setup steps after the initial implementation defines the
runtime, package manager, and required environment variables.

## Verification

Document the commands that should be run before claiming work is complete.
